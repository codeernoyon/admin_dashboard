import { Plaster, Righteous } from "next/font/google";

export const righteous = Righteous({
  weight: "400",
  subsets: ["latin"],
});
export const plaster = Plaster({
  weight: "400",
  subsets: ["latin"],
});
